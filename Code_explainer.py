age = 20
item = 5
print("age : ",age)
print("items : ", item)

age = float(age)
item = float(item)
print("age : ",age)
print("items: ", item)
output = age = 20
decimal: item = 5
float:   age = 20.0
float:   item = 5.0
WHAT IT DOES?
          we were given integers number we have to convert them into float through python programming.as it is written above first they were decimals than after output we changed them into float by adding .0 in the last f them.
